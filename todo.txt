 Practical priorities (if you invest next)
Bridge or choose one: e.g. implement ILoggerProvider that forwards to ILoggingService, or document that file logs are the source of truth.
CI: dotnet test ipdfreely.Tests/ipdfreely.Tests.csproj on Windows (and optionally Android) with coverage upload.
Lighter test TFM for pure Services tests (net10.0 only) if Android TFM is not required for those assemblies.
Smoke tests for IPdfDocumentFactory on Windows in a minimal harness, or golden-file tests for PdfOverlayExportService output bytes (you already have export tests — extend with real small PDFs if needed).